# Logging configuration for integration tests
# Java Util Logging configuration for test environment

# Default console handler configuration
.handlers=java.util.logging.ConsoleHandler
.level=INFO
java.util.logging.ConsoleHandler.level=INFO
java.util.logging.ConsoleHandler.formatter=java.util.logging.SimpleFormatter

# Specific configuration for our integration test packages
fr.julm.keycloak.providers.auth.cloudfront.it.level=INFO

# Enable debug logging for our specific test classes if needed
# fr.julm.keycloak.providers.auth.cloudfront.it.AbstractKeycloakIntegrationTest.level=FINE
